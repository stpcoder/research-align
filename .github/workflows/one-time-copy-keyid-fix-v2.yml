name: Apply Korean copy and KeyID patch

on:
  push:
    paths:
      - '.github/workflows/one-time-copy-keyid-fix-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch UI and KeyID
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          page = Path('src/app/page.tsx')
          text = page.read_text()
          replacements = {
            "Research operations, without the spreadsheet.": "실험 운영을 한 곳에서 관리하세요.",
            "연구자는 각자 로그인하고 자신의 여러 실험만 관리합니다.": "참가자 모집, 신청서 작성, 일정 조율, 연락까지 한 곳에서 관리할 수 있습니다.",
            "<h1>내 연구</h1><p className=\"muted\">한 연구자 계정에서 여러 실험을 독립적으로 운영합니다.</p>": "<h1>실험 관리</h1><p className=\"muted\">진행 중인 실험을 만들고 참가자 신청과 일정을 관리하세요.</p>",
            "샘플 연구 만들기": "샘플 실험 보기",
            "+ 새 연구": "+ 새 실험",
            "title:'새 연구'": "title:'새 실험'",
            "← 내 연구": "← 실험 목록",
            "const tabs=[['form','Form'],['responses','Responses'],['schedule','Schedule'],['contact','Contact Hub']]": "const tabs=[['form','신청서'],['responses','신청자'],['schedule','일정'],['contact','연락']]",
            "study.status==='published'?'모집 중지':'Publish'": "study.status==='published'?'모집 중지':'모집 시작'",
            "<label>연구 제목": "<label>실험 제목",
            "Google Form처럼 필요한 항목을 자유롭게 쌓습니다. 시간 선택만 scheduling-aware field입니다.": "이름, 연락처, 객관식, 시간 선택 등 필요한 항목을 추가할 수 있습니다.",
            "<h2>Responses</h2>": "<h2>신청자</h2>",
            "<span className=\"pill\">submitted</span>": "<span className=\"pill\">신청 완료</span>",
            "Study contact identity": "연구용 이메일",
            "연구별 KeyID identity를 Next.js backend에서만 사용합니다. 개인 연구자의 메일/전화번호는 참가자에게 노출되지 않습니다.": "참가자 연락에 사용할 연구 전용 이메일입니다. 연구자의 개인 이메일 주소는 참가자에게 공개되지 않습니다.",
            "KeyID 연구용 주소 연결": "연구용 이메일 연결",
            "<b>연구용 연락처</b> · email": "<b>연구용 이메일</b> ·",
            "아직 KeyID identity가 연결되지 않았습니다. 이메일 발신/수신을 사용하려면 위 버튼으로 연결하세요.": "아직 연구용 이메일이 연결되지 않았습니다. 위 버튼을 눌러 연결하면 이 화면에서 참가자와 이메일을 주고받을 수 있습니다.",
            "새 연락 thread": "새 연락",
            "설문에서 참가자가 직접 제출한 연락처만 대상으로 thread를 엽니다.": "참가자가 신청서에 입력한 이메일 또는 전화번호로 연락할 수 있습니다.",
            "Thread 만들기": "연락 시작",
            "문의 / 연락": "참가자 연락",
            "KeyID email은 Next.js server route에서 발송되고, 답장은 webhook으로 이 thread에 들어옵니다.": "보낸 이메일과 참가자의 답장이 이 화면에 함께 기록됩니다.",
            "SMS thread — inbound 수신 기록용": "SMS 메시지",
            "최신 KeyID는 persistent phone/SMS inbox를 지원하지만 공개 JS SDK의 outbound SMS 메서드가 아직 확인되지 않아 이 빌드에서는 수신 thread만 안전하게 연결합니다.": "현재 SMS는 참가자 연락처를 기록하고 수신 내역을 확인하는 용도로 사용합니다.",
          }
          for old, new in replacements.items():
            text = text.replace(old, new)
          page.write_text(text)

          participant = Path('src/components/ParticipantForm.tsx')
          text = participant.read_text()
          replacements = {
            "<span className=\"pill live\">Submitted</span><h1>응답이 제출되었습니다.</h1><p className=\"muted\">선택한 시간은 예약이 아닙니다. 연구자가 최종 일정을 확정한 뒤 입력한 연락 수단으로 안내합니다.</p>": "<span className=\"pill live\">제출 완료</span><h1>신청이 완료되었습니다.</h1><p className=\"muted\">연구자가 신청 내용을 확인한 뒤 입력하신 연락처로 일정을 안내해드립니다.</p>",
            "<span className=\"muted small\">Participant</span>": "<span className=\"muted small\">참가 신청</span>",
            "<span className=\"pill live\">Recruiting</span>": "<span className=\"pill live\">모집 중</span>",
            "<div className=\"card between row\"><div><b>제출 전 확인</b><span className=\"muted small\">가능 시간을 제출해도 즉시 예약되지는 않습니다.</span></div><button className=\"btn\">가능 시간 제출</button></div>": "<div className=\"card stack\"><div><b>입력한 내용을 확인해주세요.</b><p className=\"muted small\">선택한 시간과 연락처를 확인한 뒤 제출해주세요. 연구자가 확인 후 일정을 안내해드립니다.</p></div><button className=\"btn\" style={{width:'100%'}}>제출하기</button></div>",
            "선택 가능한 시작 시간만 표시됩니다.": "날짜별로 참여할 수 있는 시간을 선택해주세요.",
            "선택한 시간 중에서만 우선순위를 지정합니다.": "가장 선호하는 시간부터 순서대로 선택해주세요.",
          }
          for old, new in replacements.items():
            text = text.replace(old, new)
          participant.write_text(text)

          Path('src/lib/keyid.ts').write_text(r'''import crypto from 'node:crypto'
import { Buffer } from 'node:buffer'

const BASE_URL = (process.env.KEYID_BASE_URL || 'https://keyid.ai').replace(/\/$/, '')
const PKCS8_PREFIX = Buffer.from('302e020100300506032b657004220420', 'hex')

function keyPair(seedHex: string) {
  const seed = Buffer.from(seedHex, 'hex')
  if (seed.length !== 32) throw new Error('Invalid KeyID seed')
  const der = Buffer.concat([PKCS8_PREFIX, seed])
  const privateKey = crypto.createPrivateKey({ key: der, format: 'der', type: 'pkcs8' })
  const publicKey = crypto.createPublicKey(privateKey)
  return {
    privateKey: seedHex,
    publicKey: publicKey.export({ type: 'spki', format: 'der' }).subarray(-32).toString('hex'),
  }
}

function sign(message: string, privateKeyHex: string) {
  const der = Buffer.concat([PKCS8_PREFIX, Buffer.from(privateKeyHex, 'hex')])
  const key = crypto.createPrivateKey({ key: der, format: 'der', type: 'pkcs8' })
  return crypto.sign(null, Buffer.from(message), key).toString('hex')
}

async function apiFetch(path: string, init: RequestInit = {}, token?: string) {
  const response = await fetch(`${BASE_URL}${path}`, {
    ...init,
    headers: {
      'content-type': 'application/json',
      ...(token ? { authorization: `Bearer ${token}` } : {}),
      ...(init.headers || {}),
    },
    cache: 'no-store',
  })
  const raw = await response.text()
  let data: any
  try { data = raw ? JSON.parse(raw) : {} } catch { data = { error: raw } }
  if (!response.ok) throw new Error(data?.error || `KeyID HTTP ${response.status}`)
  return data
}

async function authenticate(seedHex: string) {
  const kp = keyPair(seedHex)
  const challenge = await apiFetch('/api/auth/challenge', {
    method: 'POST',
    body: JSON.stringify({ pubkey: kp.publicKey }),
  })
  const signature = sign(challenge.nonce, kp.privateKey)
  const verified = await apiFetch('/api/auth/verify', {
    method: 'POST',
    body: JSON.stringify({ pubkey: kp.publicKey, nonce: challenge.nonce, signature }),
  })
  return verified.token as string
}

export function keyIdWebhookTokenHash(webhookToken: string) {
  return crypto.createHash('sha256').update(webhookToken).digest('hex')
}

export async function provisionKeyId(seedHex: string) {
  const kp = keyPair(seedHex)
  const projectKey = process.env.KEYID_PROJECT_KEY?.trim()
  return apiFetch('/api/provision', {
    method: 'POST',
    body: JSON.stringify({
      pubkey: kp.publicKey,
      storageType: 'secrets_manager',
      ...(projectKey ? { projectKey } : {}),
    }),
  })
}

export async function getKeyIdIdentity(seedHex: string) {
  const token = await authenticate(seedHex)
  return apiFetch('/api/identity', {}, token)
}

export async function sendKeyIdEmail(seedHex: string, to: string, subject: string, body: string, threadId?: string) {
  const token = await authenticate(seedHex)
  return apiFetch('/api/send', {
    method: 'POST',
    body: JSON.stringify({ to, subject, body, displayName: 'StudyForm', ...(threadId ? { threadId } : {}) }),
  }, token)
}

export async function createKeyIdWebhook(seedHex: string, url: string) {
  const token = await authenticate(seedHex)
  return apiFetch('/api/webhooks', {
    method: 'POST',
    body: JSON.stringify({ url, events: ['message.received'] }),
  }, token)
}
''')

          provision = Path('src/app/api/keyid/provision/route.ts')
          text = provision.read_text()
          text = text.replace("createKeyIdWebhook, getKeyIdIdentity, keyIdWebhookTokenHash, provisionKeyId, requestKeyIdPhone", "createKeyIdWebhook, getKeyIdIdentity, keyIdWebhookTokenHash, provisionKeyId")
          start = text.find("    let identity = await getKeyIdIdentity(material.key_seed)")
          end = text.find("    const webhookToken = material.webhook_token", start)
          if start != -1 and end != -1:
            text = text[:start] + "    const identity = await getKeyIdIdentity(material.key_seed)\n" + text[end:]
          text = text.replace("    if (identity.phone) channels.push({ study_id: study.id, provider: 'keyid', channel: 'phone', address: identity.phone, provider_identity_id: identity.agentId || null, status: 'active', config: baseConfig } as any)\n", "")
          text = text.replace("return Response.json({ ...identity, phone: identity?.phone || phoneResult?.phone || phoneResult?.phoneNumber || null, phoneResult, webhookConfigured: true, studyTitle: study.title })", "return Response.json({ ...identity, webhookConfigured: true, studyTitle: study.title })")
          provision.write_text(text)

          package = Path('package.json')
          p = package.read_text().replace(',\n    "@keyid/sdk": "0.4.3"', '')
          package.write_text(p)
          PY

      - name: Commit changes
        shell: bash
        run: |
          git config user.name 'research-align-bot'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add src package.json
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'Polish Korean copy and align KeyID with agent-kit'
          git push
